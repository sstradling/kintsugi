[project]
name = "kintsugi-pipeline"
version = "0.1.0"
description = "Authoring pipeline for Kintsugi puzzle bundles."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
